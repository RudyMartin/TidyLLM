# py311_requirements.txt
# Comprehensive requirements for the py311 conda environment
# This includes all packages needed for the enhanced embedding system and MCP architecture

# Core AI/ML Libraries
dspy>=3.0.1,<4.0.0
litellm>=1.75.9,<2.0.0
sentence-transformers>=5.1.0,<6.0.0  # Enhanced version for better model support
torch>=2.2.2,<3.0.0
transformers>=4.55.2,<5.0.0

# AWS Services
boto3>=1.40.14
botocore>=1.40.14

# AWS/Autogluon dependencies (may not work locally on Python 3.7)
aiobotocore  # AWS async boto - may need Python 3.8+
amazon-sagemaker-jupyter-ai-q-developer  # AWS SageMaker - local dev only
amazon-sagemaker-sql-magic  # AWS SageMaker - local dev only
autogluon-common  # AutoML - may need Python 3.8+
autogluon-core  # AutoML - may need Python 3.8+
autogluon-features  # AutoML - may need Python 3.8+
autogluon-multimodal  # AutoML - may need Python 3.8+
autogluon-tabular  # AutoML - may need Python 3.8+
autogluon-timeseries  # AutoML - may need Python 3.8+

# Data Processing
pandas>=2.3.1,<3.0.0
numpy>=2.3.2,<3.0.0
scipy>=1.16.1,<2.0.0
scikit-learn>=1.7.1,<2.0.0

# File Processing
openpyxl>=3.1.5,<4.0.0
PyPDF2>=3.0.1,<4.0.0
python-docx>=1.2.0,<2.0.0
fpdf>=1.7.2,<2.0.0

# Text Processing
nltk>=3.9.1,<4.0.0
regex>=2025.7.34

# Web Framework
streamlit>=1.48.1,<2.0.0
fastapi>=0.116.1,<1.0.0

# Visualization
matplotlib>=3.10.5,<4.0.0
seaborn>=0.13.2,<1.0.0
altair>=5.5.0,<6.0.0

# Testing
pytest>=8.4.1,<9.0.0
pytest-cov>=6.2.1,<7.0.0
pytest-mock>=3.14.1,<4.0.0

# Utilities
tqdm>=4.67.1,<5.0.0
requests>=2.32.5,<3.0.0
pydantic>=2.11.7,<3.0.0
python-dotenv>=1.1.1,<2.0.0

# Additional dependencies for MCP and advanced features
datasets>=2.18.1,<3.0.0
huggingface_hub[mcp]>=0.34.4,<1.0.0
plotly>=5.21.1,<6.0.0

# Database and Vector Storage
psycopg2-binary>=2.9.9,<3.0.0  # PostgreSQL adapter
pgvector>=0.2.4,<1.0.0  # Vector extension for PostgreSQL

# MLflow for experiment tracking
mlflow>=2.8.0,<3.0.0

# QA Report Generation dependencies
PyYAML>=6.0,<7.0.0

# Core dependencies
psutil>=5.8.0,<6.0.0

# Enhanced Embedding System Dependencies
# These are already included above but listed here for clarity:
# - sentence-transformers>=5.1.0 (for embedding generation)
# - numpy>=2.3.2 (for array operations)
# - torch>=2.2.2 (backend for sentence-transformers)

# Development and Debugging
ipython>=8.0.0,<9.0.0
jupyter>=1.0.0,<2.0.0
notebook>=7.0.0,<8.0.0

# Code Quality
black>=23.0.0,<24.0.0
flake8>=6.0.0,<7.0.0
mypy>=1.0.0,<2.0.0

# Documentation
sphinx>=7.0.0,<8.0.0
sphinx-rtd-theme>=1.3.0,<2.0.0

# Note: Some packages may have version conflicts or platform-specific requirements
# If you encounter issues, try installing packages one by one or use conda for some packages
